{% extends 'base.html.twig' %}

{% block title %}
    change-password
{% endblock %}

{% block body %}
    <h1 class="text-center"> Modifier mon mot de passe</h1>
    <div class="container col-5">
        {% if notification %}
            <div class="alert alert-info">{{ notification}}</div>
          
        {% endif %}
        {{form(form)}}
        </div>

    <a href="{{path('app_account')}}" class="btn bnt-sm btn-primary mt-5 col-4">retourner sur mon compte</a>
{% endblock  %}